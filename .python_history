import os
port = int(os.environ.get("PORT", 5000))
print(port)
exit
# Now you're in Python, then type:
import os
port = int(os.environ.get('PORT', 5000))
# etc...
if __name__ == '__main__':
    import os
    port = int(os.environ.get('PORT', 5000))
    app.run(host='0.0.0.0', port=port)
l
y
()
exit
